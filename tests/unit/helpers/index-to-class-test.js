import { indexToClass } from 'nobreaks/helpers/index-to-class';
import { module, test } from 'qunit';

module('Unit | Helper | index to class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = indexToClass([42]);
  assert.ok(result);
});
